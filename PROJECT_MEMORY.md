# SBDC 项目长期记忆

最后更新：2026-09-21

## 项目目标

SBDC 的正式英文名是 `Source-Based Deep Check`，中文含义为“基于来源与证据的深度核查”。项目面向博士论文和期刊论文提供科研诚信深度检查，以证据工作台的形式发现并呈现文本复用、引用问题、统计与数据异常、图片复用和跨版本不一致，服务审查者完成复核，不替代正式调查或学术不端定性。

## 已确认决策

1. 不建设长期全文对照库。
2. 每个检查任务从待检论文引用列表出发，合法获取可用的开放全文，形成任务级临时对照库。
3. 引用范围检查是默认能力；未引用外部来源追查属于以后按需开启的补充能力，仍只构建临时语料。
4. 系统必须区分“相似/异常证据”与“抄袭/造假定性”。
5. 第一版采用 Python 模块化单体加异步 Worker，而不是微服务群。
6. 临时语义索引使用 FAISS；当前规模不引入长期向量数据库或全文搜索集群。
7. GROBID 是学术 PDF 结构化解析底座；PyMuPDF负责底层页面、坐标和图片处理；扫描页按需使用 PaddleOCR。
8. 文本检测采用词面候选、语义候选和精确片段对齐的多阶段管线，不能把向量相似度直接称为抄袭率。
9. 成熟的 R 统计工具通过隔离 Worker 复用；不为了统一语言而重写。
10. 未公开论文和原始数据默认本地或私有化处理，不发送给第三方大模型。

## 当前状态

- 已建立 `apps/web`、`apps/api`、`apps/worker` 与 `packages/domain` 工程骨架。
- 已实现数字版 PDF 的任务创建、受限上传、SHA-256、任务隔离存储、Celery 幂等调度、GROBID 解析、结构与引用覆盖率展示。
- 已建立 PostgreSQL 初始迁移和 PostgreSQL、Redis、MinIO、GROBID 本地 Compose。
- 已建立面向任意论文 PDF 的通用证据初筛阶段：全文文本块、非小型 PDF 内嵌位图和测量条件一致性规则统一生成带页码与坐标的证据项；审查者可逐项裁决并生成固定版本 PDF 报告。`Room Temperature Triggered Single Photon Emission from Self-Assembled GaN/AlN Quantum Dot in Nanowire` 只作为真实验收样本，不是规则前提。
- 主域名根路径是公众营销落地页；审查者登录与工作台分别位于 `/login`、`/workbench`。
- 已建立独立的公众投稿账号与 `/submit` 科研监督投稿入口。公众账号只能提交并查看自己的收件记录，不能访问审查工作台；审查者可在工作台查看投稿队列。
- 页脚通过产品内页面读取 SZLKlaws 的七类共享法律文件和 SBDC 产品法律补充，并展示 SZLK LTD 公司主体与官网。
- 已落地引用元数据规范化、OpenAlex 开放位置获取、安全受限 PDF 下载、任务级临时索引、跨论文连续文本对齐与可解释语义近似候选、按引用序号绑定的论断支持核对、可直接复算的统计表达式、PDF 图片局部区域筛查、来源 PDF 定位、人工裁决、版本化报告与可审计清理终态；失败任务也可清理临时资产。全文、页数、字符、索引和候选比较均有任务级预算，达到上限时披露未处理范围。目标论文仍只是验收样本。跨语言/嵌入式语义召回、更多统计格式、表格数据和原图取证仍属于后续增强，报告必须如实显示覆盖率与能力边界。

## 文档入口

- 项目总览：`README.md`
- 产品边界：`docs/01-产品定义与范围.md`
- 技术架构：`docs/03-系统架构与技术栈.md`
- 统一数据契约：`docs/05-数据模型与任务生命周期.md`
- 实施与验收：`docs/06-MVP实施计划与验收.md`
- 决策记录：`docs/DECISIONS.md`
- 本地运行：`docs/08-本地运行与验证.md`
