# SBDC 项目长期记忆

最后更新：2026-08-27

## 项目目标

SBDC 面向博士论文和期刊论文提供科研诚信深度检查。系统以证据工作台的形式发现并呈现文本复用、引用问题、统计与数据异常、图片复用和跨版本不一致，服务审查者完成复核，不替代正式调查或学术不端定性。

## 已确认决策

1. 不建设长期全文对照库。
2. 每个检查任务从待检论文引用列表出发，合法获取可用的开放全文，形成任务级临时对照库。
3. 引用范围检查是默认能力；未引用外部来源追查属于以后按需开启的补充能力，仍只构建临时语料。
4. 系统必须区分“相似/异常证据”与“抄袭/造假定性”。
5. 第一版采用 Python 模块化单体加异步 Worker，而不是微服务群。
6. 临时语义索引使用 FAISS；当前规模不引入长期向量数据库或全文搜索集群。
7. GROBID 是学术 PDF 结构化解析底座；PyMuPDF负责底层页面、坐标和图片处理；扫描页按需使用 PaddleOCR。
8. 文本检测采用词面候选、语义候选和精确片段对齐的多阶段管线，不能把向量相似度直接称为抄袭率。
9. 成熟的 R 统计工具通过隔离 Worker 复用；不为了统一语言而重写。
10. 未公开论文和原始数据默认本地或私有化处理，不发送给第三方大模型。

## 当前状态

- 已建立 `apps/web`、`apps/api`、`apps/worker` 与 `packages/domain` 工程骨架。
- 已实现数字版 PDF 的任务创建、受限上传、SHA-256、任务隔离存储、Celery 幂等调度、GROBID 解析、结构与引用覆盖率展示。
- 已建立 PostgreSQL 初始迁移和 PostgreSQL、Redis、MinIO、GROBID 本地 Compose。
- 文本查重、引用全文获取、统计检查、图片取证、人工裁决与报告仍属于后续切片，当前页面不声称完成这些检查。

## 文档入口

- 项目总览：`README.md`
- 产品边界：`docs/01-产品定义与范围.md`
- 技术架构：`docs/03-系统架构与技术栈.md`
- 统一数据契约：`docs/05-数据模型与任务生命周期.md`
- 实施与验收：`docs/06-MVP实施计划与验收.md`
- 决策记录：`docs/DECISIONS.md`
- 本地运行：`docs/08-本地运行与验证.md`
