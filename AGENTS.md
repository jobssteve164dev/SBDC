# AGENTS.md

## 项目目标

交付一个供真实审查者使用的论文科研诚信证据工作台。产品价值是帮助用户完成“发现问题、查看依据、作出复核决定”，不是向用户展示内部模型、对象或工程流程。

## 开始工作前

- 先读 `README.md`、`PROJECT_MEMORY.md` 和与任务相关的 `docs/` 文件。
- 当前实现与测试高于蓝图；如果代码与文档冲突，先确认现实并更新对应决策，不能把蓝图当成已实现。
- 未经用户明确要求，不建设长期全文库，不把任务临时全文写入共享索引。
- 不修改 SoloMap 路线图、阶段状态或交接记录。

## 产品硬边界

- 用户上传一篇待检论文，系统围绕这篇论文的引用列表建立任务级临时对照库。
- 默认检查范围是引用范围；外部未引用来源追查只能作为显式、按需能力，且仍不得沉淀长期全文。
- 系统输出“证据”和“待复核结论”，不得自动输出作者存在造假、抄袭或主观故意。
- PDF-only、原始数据包、调查材料包是不同证据等级，界面和报告必须如实标注能力边界。
- 引用下载必须尊重开放获取与授权状态，不得绕过付费墙。

## 实现规则

- 先完成最小纵向闭环：上传 → 解析 → 引用解析与开放全文获取 → 临时索引 → 文本证据 → 人工裁决 → 报告 → 生命周期收口。
- 生产数据库必须使用 GitOps 托管数据库生命周期，在统一治理面完成创建、绑定、凭据、网络、备份、迁移和状态展示；业务 Compose 中的 PostgreSQL 仅限本地开发，不得进入生产发布路径或充当生产数据库。
- Python 是检查与编排主语言；成熟 R 统计工具保留在隔离 Worker 中，不做无价值重写。
- 每个检测器输出统一的 `EvidenceItem`，不能直接改变案件最终结论。
- 任何最终证据必须携带待检位置、对照来源、原始片段或区域、方法与版本、置信度、限制和可复核资产。
- 大模型只能辅助论断拆分、证据重排和解释；无引用原文时不得生成支持性判断。
- 临时全文、向量和派生图片按 `task_id` 隔离；清理必须可审计，失败不能伪装为已清理。

## 验证规则

- 文档纯改动不运行代码测试，但必须检查链接、文件清单和关键边界是否一致。
- PDF 解析改动必须用真实数字版和扫描版样本验证最终结构与坐标。
- 检测器验证必须分别覆盖真阳性、合理引用、常见方法套话和无法取得全文等负向情况。
- 模板、报告和可视化改动必须验证最终生成物，而非只验证源模板。
- 面向用户的页面和报告交付前必须二次检查：不暴露工程对象，不把相似度误写成抄袭率，不把异常误写成造假。

## 安全

- 不在日志、URL、任务名或错误信息中泄露论文正文、原始数据、凭据或个人敏感信息。
- 文件类型、大小、解压量、页数和解析资源必须在首次消费前限界。
- 统计代码、上传脚本和文档内嵌对象均按不可信输入处理，在隔离容器内运行；首期默认不执行用户上传代码。
- 不删除非当前任务创建的文件或对象。
